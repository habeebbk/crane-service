name: crane_service_app
description: A Crane & Earthmover Service management application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  pdf: ^3.10.4
  printing: ^5.11.0
  intl: ^0.18.1
  path_provider: ^2.1.1
  share_plus: ^7.1.0
  cupertino_icons: ^1.0.2
  firebase_core: ^4.2.1
  cloud_firestore: ^6.1.0
  firebase_analytics: ^12.0.4
  firebase_auth: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
